import { Intersectable } from './intersectable';

describe('Intersectable', () => {
  it('should create an instance', () => {
    const directive = new Intersectable();
    expect(directive).toBeTruthy();
  });
});
